apiVersion: v1
kind: Service
metadata:
  name: content-service # Nombre DNS para este servicio
spec:
  selector:
    app: content-service
  ports:
    - protocol: TCP
      port: 8020
      targetPort: 8020
  type: ClusterIP