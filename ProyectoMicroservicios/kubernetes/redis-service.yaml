apiVersion: v1
kind: Service
metadata:
  name: redis-db # Nombre DNS para conectar desde metrics-service
spec:
  selector:
    app: redis-db
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP