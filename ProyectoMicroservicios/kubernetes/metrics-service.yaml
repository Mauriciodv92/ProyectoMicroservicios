apiVersion: v1
kind: Service
metadata:
  name: metrics-service # Nombre DNS para este servicio
spec:
  selector:
    app: metrics-service
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP